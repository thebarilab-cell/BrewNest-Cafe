<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand" asp-controller="Home" asp-action="Index">
             BrewNest<span>Cafe</span>
        </a>
        <button class="navbar-toggler border-0 shadow-none text-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <i class="bi bi-list fs-2"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto align-items-center">
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Menu" asp-action="Index">Menu</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="About" asp-action="Index">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Gallery" asp-action="Index">Gallery</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto align-items-center pb-3 pb-lg-0">
                @if (User.IsInRole("Admin"))
                {
                    <li class="nav-item me-lg-3 mb-2 mb-lg-0">
                        <a class="btn-premium btn-premium-outline" style="padding: 10px 20px; font-size: 0.85rem;" asp-controller="Admin" asp-action="Dashboard">
                            Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                            <button type="submit" class="btn-premium btn-premium-gold" style="padding: 10px 20px; font-size: 0.85rem;">
                                Logout
                            </button>
                        </form>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="btn-premium btn-premium-gold" style="padding: 10px 20px; font-size: 0.85rem;" asp-controller="Account" asp-action="Login">
                            <i class="bi bi-person me-2"></i>Sign In
                        </a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>